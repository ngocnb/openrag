# 2026-03-25
- I want to change frontend from nextjs to react + vite for faster loading.
  - make new folder frontend-vite
  - clone the source from frontend with all the current features
  - make sure that everything is tested
  - add it to docker-compose
  - add it to "uv run openrag"
